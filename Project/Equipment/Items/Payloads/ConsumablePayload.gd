extends ItemPayload
class_name ConsumablePayload

@export var heal_amount: float = 0.0

@export var restore_resource_type: ResourceType.Id = ResourceType.Id.MANA

@export var restore_amount: float = 0.0

@export var cooldown: float = 0.0
